
import { SessionStore, WsSession } from '../../src/SessionStore';
import WebSocket from 'ws';
import { expect } from 'chai';
import { UserDto } from '../../src/brokers/dto/user.dto';

describe('Test class SessionStore', () => {

  it('Create new session (method `createSession`)', () => {

  });

  it('test method `getSessionById`', () => {
    return true;
  });

  it('test method `getSessionByToken`', () => {
    return true;
  });

  it('test method `getSessionByUser`', () => {
    return true;
  });

  it('Session store created', () => {
    return true;
  });
});