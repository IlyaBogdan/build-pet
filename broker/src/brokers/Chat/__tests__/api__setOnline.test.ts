import { expect } from 'chai';

describe('Test for API methods of ChatBroker (`setOnline`)', () => {

  it('User connected and in session was setted online status', () => {
    expect(true);
  });
});